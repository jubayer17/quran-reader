services:
  server:
    build: ./server
    ports:
      - "3001:3001"

  client:
    build: ./client
    ports:
      - "3000:3000"
    environment:
      - QURAN_API_URL=http://server:3001
    depends_on:
      - server
